geonaute_activity_test:
    path:     /test.json
    defaults: { _controller: GeonauteActivityBundle:Default:index }

geonaute_activity_post:
    path:     /activity.json
    defaults: { _controller: GeonauteActivityBundle:Default:post }
    methods: POST

geonaute_activity_list:
    path:     /stats/{product_id}.json
    defaults: { _controller: GeonauteActivityBundle:Default:productList }
    methods: GET

